services:
  web:
    build: .
    ports:
      - "6745:6745"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - PORT=${PORT:-6745}
    restart: unless-stopped
    container_name: sbnm-poly-web
