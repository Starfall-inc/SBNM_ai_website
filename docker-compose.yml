services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - PORT=${PORT:-3000}
    restart: unless-stopped
    container_name: sbnm-poly-web
